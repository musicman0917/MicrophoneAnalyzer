<Window x:Class="MicrophoneAnalyzer.UI.DeviceSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Input Device" Height="240" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#FF1A1A1E" Foreground="White" FontFamily="Segoe UI">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Input Device (USB Interface)" Margin="0,0,0,4"/>
        <ComboBox x:Name="DeviceCombo" Grid.Row="1" Margin="0,0,0,12"
                  DisplayMemberPath="FriendlyName"
                  SelectionChanged="DeviceCombo_SelectionChanged"/>

        <TextBlock Grid.Row="2" Text="Channel (e.g. the DLZ channel your DynaCaster is routed to)"
                   TextWrapping="Wrap" Margin="0,0,0,4"/>
        <ComboBox x:Name="ChannelCombo" Grid.Row="3" VerticalAlignment="Top"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Width="80" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="OK" Width="80" Click="Ok_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
